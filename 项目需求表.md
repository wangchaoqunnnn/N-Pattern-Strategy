# 项目需求表 —— N字战法自动选股与自我执行交易系统

> 生成日期：2026-09-07　|　版本：v1.0　|　状态：✔ 已实现 / ◐ 部分(有说明) / ✘ 未实现

## 一、功能需求（9 项）

| 序号 | 需求描述 | 实现方式(模块/文件) | 界面位置 | 验收方法 | 状态 |
|------|---------|--------------------|---------|---------|------|
| F1 | 实时自动筛选符合条件的个股 → **推荐买入池**；用户可手动增加/删除 | `engine/scan.py`（N字形态量化判定）；`engine/pool.py`（自动入池/过期出池/手动增删）；`service.py` 盘中定时全市场扫描 | 「推荐买入池」页：自动行(来源=自动)+手动添加/移除按钮 | 交易日盘中观察：出现点火+缩量回调形态个股自动入池；手动搜索添加/移除立即生效 | ✔ |
| F2 | 给出**可执行的交易系统**，对池内股票自主判断买卖；记录买卖点与理由；统计盈亏比/成功率；**可回测可复盘** | `engine/strategy.py`（版本+人读规则+量化参数）；`engine/trader.py`（买卖执行、理由落库）；`engine/stats.py`（成功率/盈亏比/权益/月度）；`engine/backtest.py`（回测）；`engine/review.py`（复盘） | 「交易系统」页可读规则与参数；「交易系统买卖池」买卖理由；「复盘统计」统计卡与曲线；「回测」页 | 系统开仓后可在买卖流水看到买入价/理由/版本号；统计页随平仓更新成功率与盈亏比；发起一段区间回测得到明细 | ✔ |
| F3 | 创建**交易系统买卖池**：自行买入操作全部入池，严格执行系统 | `positions`（持仓/平仓）+ `executions`（买卖流水）双表；`trader.py` 所有下单/平仓必经 `execute_buy/execute_sell`，触发条件逐条对应版本参数 | 「交易系统买卖池」页三个子页：当前持仓/历史平仓/买卖操作流水 | 买卖操作流水含：时间、方向、价格、股数、手续费、模式、**版本号**、理由 | ✔ |
| F4 | 每日**收盘后复盘**交易记录，分析买入失败原因并写入备注 | `service.close_pass()`（15:00后自动）→ `review.run_daily_review()` 自动归因：early_entry/fake_breakout/bad_environment/pattern_break 等写入 `positions.failure/failure_note/note` | 「复盘统计」页复盘记录(每日)内容与备注；平仓明细"失败备注"列 | 收盘流程自动执行或页面按钮触发后，查看当日复盘报告与失败单备注 | ✔ |
| F5 | **连续5天**盈亏比<0.6 且 成功率<40% → 系统立即自行识别并**自我优化** | `optimizer.rule5_window_state()` 滚动窗口监测（连续5个交易日、每窗口≥3笔样本、盈亏比<0.6且成功率<40%）；达标后 `auto_optimize('rule5_auto')` 按失败归因生成新版本并**自动激活** | 「交易系统」页：规则5监测窗口表 + 自优化记录 | 页面可查看近5日各窗口统计与"达标/不达标"判定；触发后在版本历史新增自动优化版本并激活 | ✔ |
| F6 | 每月**复盘当月**数据，判断是否需要优化，需要则自行优化 | `review.run_monthly_review()` + `optimizer.monthly_optimize()`；月末最后一个交易日收盘流程自动触发；判定规则：平仓≥3笔且(成功率<50% 或 盈亏比<1 或 月收益<0) → 自动优化 | 「复盘统计」月度统计/月度复盘记录；「交易系统」自优化记录 | 月末运行后出现"月度复盘"记录与优化版本(如触发)；亦可页面手动运行月度复盘 | ✔ |
| F7 | 记录**历史交易系统版本**，用户可手动选择任一历史版本执行 | `versions` 表存全量参数快照+可读规则；`strategy.set_active()` 切换当前执行版本；买卖记录引用当时的版本号 | 「交易系统」页版本历史列表 + 「切换执行」按钮 | 创建/自动优化多个版本后任选历史版本切换，此后新买卖流水记录新版本号 | ✔ |
| F8 | 买卖池操作记录**所依据的版本号** | `executions.version_id/version_no`、`positions.version_id/version_no`；切换版本后存量持仓仍保留原版本字段，卖出按其记录版本回写 | 「交易系统买卖池」所有子表均含"版本"列 | 每行流水/持仓/平仓可见 vN 版本号 | ✔ |
| F9 | 选股范围为**沪深主板/科创板/创业板等全市场** | `market.SCREEN_BOARDS`=[沪主板,深主板,科创板,创业板]；新浪全市场列表→腾讯日K缓存→内存扫描；剔除ST/退市/B股/北交所(可配置) | 「仪表盘」显示全市场数与扫描范围；「推荐买入池」板块列 | 科创板(688)/创业板(300)个股可被自动筛选入池并交易 | ✔ |

## 二、代码/工程需求（5 项）

| 序号 | 需求 | 实现方式 | 验收 | 状态 |
|------|------|---------|------|------|
| C1 | 项目完成后给出**项目需求表** | 本文档 | 需求与实现逐条对应 | ✔ |
| C2 | 前后端**不得使用静态/固定/绝对地址** | ①前端仅同源相对请求(`/api/*`、`app.js`)，无CDN/无外链；②后端代码零硬编码URL/IP/路径：数据源地址唯一存放于 `server/providers.json`（可换源不改码），host/port来自环境变量或 `server/config.json`，数据/日志目录以模块自身 `Path(__file__)` 相对解析并支持 `NSTRAT_DATA` 覆盖；回测/图表无任何前端地址配置 | `grep -rn "http://\|https://" server/ web/ *.py` 仅应命中 providers.json 与文档；启动后界面功能可用 | ✔ |
| C3 | 上涨股票**红色**、下跌**绿色**（名称/现价/涨跌额/涨跌幅等） | 前端统一 `updown()/pn()/nm()` 色彩函数（CSS 变量 `--up:#e02f2f 红 / --down:#00a854 绿`） | 任意表格：正数红色、负数绿色 | ✔ |
| C4 | 可部署于云服务器 **/root/** | `deploy/deploy.sh`：默认安装 `/root/NPatternStrategy`，venv+依赖+systemd 常驻+开机自启，日志到 `data/logs/`；所有路径由脚本变量/相对定位生成 | 在 /root 服务器执行 `bash deploy/deploy.sh` 后 `systemctl status nstrategy` 正常 | ✔ |
| C5 | 云端**交易日交易时间内实时监控/刷新** | 调度器按北京时间（UTC+8 固定）运行：交易时段每5秒行情快照+卖出检查/尝试买入，每60秒全市场扫描刷新推荐池（间隔可配置）；交易日历由指数K线自动判定，周末/节假日自动跳过；收盘后自动复盘/自优化/增量同步 | 页面仪表盘"最近扫描/最近快照"时间持续跳动；收盘流程自动执行并产生复盘 | ✔ |

## 三、附加设计说明

1. **交易系统规则（v1，人读版）** 见「交易系统」页面；量化口径见 README「四、N字形态判定」。
2. **失败归因字典**：early_entry（买点过早/趋势走弱）、fake_breakout（假突破追高）、stop_too_tight（止损过近被洗）、bad_environment（大盘拖累）、pattern_break（形态破位）、data_suspend（停牌/异常）、other。归因结果写入持仓备注并驱动优化方向。
3. **纸面交易口径**：初始虚拟资金 1,000,000 元；单票≤20%、同时≤5只、每日新开≤3只、同股冷却10个交易日；含佣金(买万2.5/卖万7.5含印花税近似)与滑点0.1%。
4. **规则5语义**：成功率=盈利平仓/已平仓；盈亏比=平均盈利%/平均亏损%；窗口=近5个交易日滚动；要求窗口平仓≥3笔以避免无样本误触发；数据不足自动顺延，日志与监测面板可见。
5. **风险提示**：本系统输出仅为研究/演示，不构成投资建议；回测与纸面结果不代表真实收益。
